# دليل شرح المشروع — AI Learning Hub

الدليل ده مكتوب عشان تقدري تشرحي أي ملف في المشروع بثقة، وتردي على أسئلة
الدكتور بمنطق واضح مش حفظ. كل قسم فيه: **إيه اللي بيعمله**، **ليه موجود**،
و**لو الدكتور سأل...** إجابات جاهزة.

---

## 🏗️ نظرة عامة (لو سألك "احكيلي عن المشروع بسرعة")

> "المشروع ده RAG Assistant — نظام بيرد على أسئلة عن الـ AI/NLP بالاعتماد
> على documentation حقيقية من 5 مصادر (LangChain, Sentence-Transformers,
> FAISS, scikit-learn, NLTK)، مش من معرفة الموديل نفسه. الفكرة إن أي إجابة
> لازم تكون **مُثبتة بمصدر حقيقي**، مش hallucination."

الـ pipeline بالكامل:
```
جمع البيانات → تنظيف → تقطيع (chunking) → تحويل لأرقام (embeddings)
→ تخزين في vector database → استرجاع (retrieval) → توليد إجابة (LLM)
→ تقييم الجودة بالأرقام
```

كل مرحلة ملف رقمه بالترتيب (`00` لحد `09`)، عشان يبين إن العملية **خط
إنتاج (pipeline)** مش سكريبت واحد عشوائي.

---

## 📄 `sources.json`

**إيه اللي فيه:** ليستة بالمواقع اللي هنجمع منها البيانات — كل مصدر فيه
`name` (اسمه)، `start_url` (منين نبدأ)، `allowed_path_prefix` (نطاق
الجمع)، `max_pages` (حد أقصى).

**ليه مصمم كده:** بدل ما أي موقع جديد يحتاج تعديل في الكود، بس بتضيفي
عنصر جديد في الملف ده. ده بيسموه **separation of config from code**
(فصل الإعدادات عن منطق البرنامج).

**لو سأل "ليه `allowed_path_prefix` منفصل عن `start_url`؟"**
> "لأن أحيانًا الصفحة اللي بنبدأ منها بتعمل redirect لصفحة تانية (زي ما
> حصل مع LangChain لما نقلوا الموقع)، فلازم نفرّق بين 'منين بدأنا' و'فين
> النطاق المسموح نجمع منه' عشان منجريش ورا الموقع كله بالغلط."

---

## ⚙️ `utils/config.py`

**إيه اللي فيه:** نقطة مركزية وحيدة تقرأ فيها كل الإعدادات (من `.env` أو
من Streamlit Secrets وقت النشر). أي ملف تاني بيستورد منها بدل ما كل ملف
يقرأ الإعدادات لوحده.

**ليه مصمم كده:** لو عايزة تغيّري إعداد (زي حجم الـ chunk أو موديل الـ
embeddings)، بتغيّريه في **مكان واحد** بس، مش في 9 ملفات مختلفة.

**لو سأل "إيه الفرق بين .env و Secrets؟"**
> "`.env` ملف محلي على الجهاز، بيحتوي على مفاتيح API الحساسة، ومتجاهل من
> Git عمدًا عشان ميترفعش على GitHub. لو نشرنا الموقع أونلاين، بنستخدم
> Streamlit Secrets بدل، وهي آلية مشابهة بس مُدارة من المنصة نفسها."

---

## 📥 `00_data_collection.py` — المرحلة صفر: الجمع

**إيه اللي بيعمله:**
1. بيدخل على كل موقع في `sources.json`
2. بيتابع أي redirect لوحده (`resolve_start_url`)
3. بيعمل **BFS** (Breadth-First Search) — يجمع كل الروابط الداخلية اللي
   جوه النطاق المسموح
4. بيزور كل صفحة، يشيل الـ navigation/footer/sidebar، ويسيب المحتوى
   الأساسي بس
5. بيحفظ كل صفحة كملف `.txt` في `data/raw/<اسم المصدر>/`

**ليه فيه `fetch_with_retry`:**
> "لأن بعض المواقع (زي Hugging Face) بترفض الطلبات السريعة برسالة 429 —
> Too Many Requests. الدالة دي بتستنى وتعيد المحاولة بدل ما تستسلم على
> طول، وبتحترم هيدر `Retry-After` لو السيرفر بعته."

**لو سأل "إيه هو BFS وليه استخدمتيه؟"**
> "BFS معناها إننا بنزور الروابط دور بدور (طابور)، مش بالعمق. ده بيضمن
> إننا نغطي أكبر عدد صفحات متنوعة قبل ما نستهلك الـ `max_pages` بدل ما
> نغرق جوه فرع واحد عميق من الموقع."

---

## 📑 `01_documents.py` — المرحلة الأولى: تحويل لـ Documents

**إيه اللي بيعمله:** بيقرا كل ملفات `.txt` من كل المصادر، ويحوّلهم لـ
كائنات `Document` (من مكتبة LangChain) — كل واحد فيه `page_content`
(النص) و`metadata` (العنوان، الرابط، اسم المصدر). بيحفظهم كلهم في ملف
واحد: `documents.jsonl`.

**لو سأل "ليه JSONL مش مجلد فيه آلاف الملفات؟"**
> "JSONL (JSON Lines) بيخلي كل الداتا في ملف واحد سهل القراءة والمعالجة،
> بدل ما نتعامل مع آلاف الملفات الصغيرة اللي بتبقى بطيئة وصعبة التتبع."

---

## 🧹 `02_preprocessing.py` — المرحلة الثانية: التنظيف

**إيه اللي بيعمله:** بيشيل السطور المتكررة (زي "Copy Page", "Search")،
بينضّف المسافات الزيادة، وبيرمي أي صفحة قصيرة جدًا بعد التنظيف (أقل من
200 حرف).

**نقطة مهمة جدًا لو سأل "ليه معملتيش lowercase أو إزالة punctuation؟"**
> "لأن التنظيف ده كان هيفيد لو كنا بنعمل classification تقليدي. لكن في
> RAG، النص المُسترجع بيتحط جوه الـ prompt ويتقرا من موديل لغوي — فأي
> تبسيط زيادة (زي lowercase) ممكن **يضر** جودة الفهم والـ embeddings.
> التنظيف هنا **structural** (نضافة شكل الصفحة) مش **linguistic**
> (تبسيط الكلمات)."

---

## ✂️ `03_chunking.py` — المرحلة الثالثة: التقطيع

**إيه اللي بيعمله:** بيقسّم كل document لقطع (chunks) بحجم 200 كلمة، مع
overlap (تداخل) 40 كلمة بين كل chunk واللي بعده.

**لو سأل "ليه بنقطّع النص أصلًا؟"**
> "لأن الـ LLM مش بيقدر يقرا كتاب كامل مرة واحدة (حدود الـ context
> window)، وكمان لو بحثنا في نص طويل، هنجيب معلومات كتير مش لازمة.
> التقطيع بيخلينا نجيب بس الجزء الأكثر صلة بالسؤال."

**لو سأل "ليه فيه overlap (تداخل)؟"**
> "عشان لو معلومة مهمة قاعدة بالظبط على الحد بين chunk وتاني، الـ overlap
> بيضمن إنها متتقطعش وتضيع. من غير overlap، ممكن جملة تتقسم نص فـ chunk
> ونص في التاني وتفقد معناها."

---

## 🔢 `04_embeddings.py` — المرحلة الرابعة: التحويل لأرقام

**إيه اللي بيعمله:** بيستخدم موديل `all-MiniLM-L6-v2` (من مكتبة
Sentence-Transformers) عشان يحوّل كل chunk لـ **vector** (مصفوفة أرقام
بحجم 384 رقم)، بحيث النصوص المتشابهة معنويًا تبقى أرقامها قريبة من بعض.

**لو سأل "إيه الفرق بين embeddings والبحث بالكلمات (keyword search)؟"**
> "البحث بالكلمات بيدور على تطابق حرفي — لو السؤال قال 'car' مش هيلاقي
> صفحة فيها 'automobile'. الـ embeddings بتفهم **المعنى**، فسؤال عن
> 'car' ممكن يجيب نتيجة عن 'vehicle' لأن معانيهم قريبة من بعض في الفضاء
> الرياضي (vector space)."

**لو سأل "ليه `normalize_embeddings=True`؟"**
> "عشان نقدر نستخدم **dot product** بدل **cosine similarity** الكامل،
> وده أسرع حسابيًا مع نفس النتيجة الرياضية بالظبط لما المتجهات متطبّعة
> (normalized)."

---

## 🗄️ `05_create_vector_store.py` — المرحلة الخامسة: التخزين

**إيه اللي بيعمله:** بياخد الـ chunks + الـ embeddings ويخزّنهم في
**ChromaDB** (قاعدة بيانات متخصصة في البحث عن أقرب المتجهات).

**لو سأل "ليه ChromaDB مش أي قاعدة بيانات عادية (زي MySQL)؟"**
> "قواعد البيانات العادية مبنية للبحث بالتطابق الدقيق (WHERE id = 5).
> لكن احنا محتاجين نسأل 'إيه أقرب 5 نتايج معنويًا لسؤالي؟' — ده بحث في
> فضاء رياضي متعدد الأبعاد، ودي بالظبط وظيفة الـ vector databases زي
> Chroma أو FAISS."

---

## 🔍 `06_retrieve_context.py` — المرحلة السادسة: الاسترجاع (Hybrid)

**إيه اللي بيعمله:** بيجمع بين طريقتين بحث مختلفين:
- **BM25** (بحث لغوي كلاسيكي، بيعتمد على تكرار الكلمات المضبوطة)
- **Embeddings** (بحث معنوي، من مرحلة 04)

وبيدمجهم بمعادلة وزن: `hybrid_score = alpha × semantic + (1-alpha) × lexical`

**لو سأل "ليه مش embeddings بس؟ هي أقوى مش كده؟"**
> "الـ embeddings ممتازة في فهم المعنى، لكنها ضعيفة أحيانًا مع **أسماء
> دقيقة** زي أسماء الدوال (`create_agent`) أو الأكواد. الـ BM25 بيمسك
> التطابق الحرفي الدقيق ده كويس. الجمع بينهم (**hybrid**) بيدّي أفضل ما
> في الطريقتين — وده أثبتناه فعليًا بالتقييم في `08_evaluate_retrieval.py`
> (الـ hybrid كان عنده أعلى MRR)."

**لو سأل "إيه هو الـ HYBRID_ALPHA بتاعك وليه القيمة دي؟"**
> "قيمته 0.8، يعني بنديّ وزن أكبر للـ semantic (80%) و20% بس للـ lexical.
> ده لأن أسئلة المستخدمين بتيجي بصيغة طبيعية ('How do I...') مش كلمات
> مفتاحية، فالمعنى أهم من التطابق الحرفي هنا. لو الأسئلة كانت تقنية جدًا
> (زي أسماء functions)، كنت هخلّي القيمة أقرب لـ 0.5."

---

## 🧩 `06b_build_context.py` — المرحلة 6ب: بناء السياق

**إيه اللي بيعمله:** بياخد نتايج الاسترجاع (اللي ممكن تكون فيها تكرار أو
معلومات قديمة) وبيعمل:
1. **Filter** — يشيل أي معلومة قديمة (`is_current=False`)
2. **Dedupe** — يشيل الـ chunks المتشابهة جدًا مع بعض
3. **Sort** — يرتب حسب الأهمية
4. **Budget** — يوقف عند حد معين من الكلمات عشان الـ prompt متكبرش أوي

**لو سأل "ليه المرحلة دي منفصلة عن الاسترجاع؟"**
> "لأن الاسترجاع بيجيب 'مرشحين محتملين'، مش سياق جاهز للاستخدام. الفصل
> ده مبدأ هندسي اسمه **separation of concerns** — كل مرحلة ليها مسؤولية
> واحدة واضحة، فلو حصلت مشكلة (زي تكرار المعلومة في الإجابة)، أعرف
> بالظبط في أنهي طبقة أدور."

---

## ✍️ `07_generate_answer.py` — المرحلة السابعة: توليد الإجابة

**إيه اللي بيعمله:**
1. بيبني الـ **prompt** من السياق + السؤال، بأسلوب من 6 (`weak`,
   `better`, `strict`, `code_examples`, `explanation_only`, `code_only`)
2. بيبعته لـ **Gemini** أو **OpenAI** (حسب `.env`)
3. بيستخرج الاستشهادات الحقيقية (`[Source: title]`) من نص الإجابة
   ويعرض **بس** المصادر اللي فعلًا اتذكرت (مش كل الـ context اللي اتبعت)
4. بيقيس الوقت وعدد الـ tokens وثقة الإجابة (confidence)

**لو سأل "إيه الفرق بين أساليب الـ prompt؟"**
> "`weak` بس بتقول 'جاوب على السؤال ده' — أسهل بس عرضة للـ hallucination.
> `better` (المستخدم افتراضيًا) بتفرض قواعد صارمة: جاوب من السياق بس،
> واذكر مصدر كل جملة. `strict` بتطلب كمان تقييم ثقة صريح (high/low) في
> نهاية الإجابة. والتلاتة الجداد (`code_examples`, `explanation_only`,
> `code_only`) بيحددوا **شكل المحتوى** نفسه مش بس أسلوب الاستشهاد —
> تفاصيلهم في القسم اللي بعد كده."

### 🆕 أوضاع الإجابة المتخصصة (Multi-Mode Answer Generation)

عشان المستخدم يقدر يختار الشكل اللي يناسبه، أضفنا 3 أوضاع إضافية،
واختيار الوضع بيأثّر على **مرحلتين** مش بس الـ prompt:

| الوضع | الـ Prompt | الـ Context Builder |
|---|---|---|
| `code_examples` | شرح نثري + كود لو موجود في السياق | بيفضّل (يقدّم) الـ chunks اللي فيها كود |
| `explanation_only` | شرح نثري بس، ممنوع أي كود حتى لو موجود في السياق | بيأخّر/يستبعد الـ chunks اللي فيها كود |
| `code_only` | كود بس، من غير أي فقرات شرح | بيفضّل الـ chunks اللي فيها كود (زي `code_examples`) |

**التنفيذ التقني (لو الدكتور سأل "إزاي عملتيها؟"):**
> "أضفت دالة `code_density()` في `06b_build_context.py` بتحسب نسبة
> السطور اللي شكلها كود (باستخدام regex بيدور على أنماط زي ```` ``` ````،
> `def `, `class `, `import`, `=>`...) في كل chunk. بعدين دالة
> `rerank_by_code_preference()` بتستخدم النسبة دي كـ 'bonus' أو 'penalty'
> فوق الـ `hybrid_score` الأصلي بتاع الاسترجاع، حسب الوضع المختار. يعني
> عندي دلوقتي **context builder ثاني** بيرتب المعلومات بشكل مختلف حسب
> نوع الإجابة المطلوبة، مش بس نفس الترتيب دايمًا."

**لو سأل "ليه معملتيش retriever منفصل تمامًا بدل ما تعدّلي الترتيب بس؟"**
> "لأن الاسترجاع نفسه (BM25 + embeddings) لسه بيلاقي **نفس المرشحين
> المحتملين** بغض النظر عن شكل الإجابة المطلوب — الفرق بس في **إمتى
> نديهم أولوية**. لو عملت retriever منفصل بالكامل، كنت هكرر منطق كتير
> من غير داعي؛ إعادة الترتيب (re-ranking) حل أخف وأنضف لنفس المشكلة."

**التوافق مع الكود القديم (backward compatibility):**
> "الباراميتر الجديد `prefer_code` في `build_context()` قيمته
> الافتراضية `None`، يعني أي كود قديم بينادي الدالة من غير الباراميتر
> ده (زي `08_evaluate_retrieval.py` و `09_log_failures.py`) بيشتغل
> بالظبط زي الأول من غير أي تغيير في السلوك."

**لو سأل "إزاي بتضمني إن الموديل مش بيختلق معلومات (hallucination)؟"**
> "3 حاجات: (1) الـ prompt بيقول صراحة 'استخدم السياق بس، متستخدميش
> معرفة خارجية'. (2) لو مفيش context كفاية، الموديل بيتقال له يقول 'معنديش
> معلومات كفاية' بدل ما يخترع. (3) أي جملة في الإجابة لازم يكون معاها
> `[Source: ...]`، وأنا بتحقق إن المصدر ده فعلًا موجود في الـ context
> اللي اتبعت، مش اسم مُختلق."

---

## 📊 `08_evaluate_retrieval.py` — التقييم بالأرقام

**إيه اللي بيعمله:** بيشغّل نفس الأسئلة على 3 طرق استرجاع مختلفة
(BM25 بس / Embeddings بس / Hybrid)، ويحسب:
- **Precision@K** — من الـ chunks اللي رجعت، كام نسبة فعلًا مفيدة؟
- **Recall@K** — من كل المعلومات المفيدة الموجودة، كام نسبة لقيناها؟
- **Hit Rate@K** — هل لقينا حاجة مفيدة أصلًا؟ (نعم/لا)
- **MRR (Mean Reciprocal Rank)** — الإجابة الصح ظهرت في مركز قد إيه؟

**لو سأل "ليه MRR مهم بالذات؟"**
> "لأنه بيعكس تجربة المستخدم الحقيقية. لو الإجابة الصح موجودة بس في
> المركز الخامس، المستخدم هيحس إن النظام بطيء أو مش دقيق، حتى لو
> Recall عالي. MRR بيكافئ إن الإجابة الصح تكون **الأولى**."

---

## 🩺 `09_log_failures.py` — تتبّع الأخطاء

**إيه اللي بيعمله:** بيشغّل كل الأسئلة، ولو أي مرحلة فشلت (retrieval,
context, generation)، بيسجّل **في أنهي طبقة بالظبط** حصلت المشكلة، في
`evaluation/failure_log.csv`.

**لو سأل "ليه مش كفاية إننا نعرف إن الإجابة غلط؟"**
> "لأن 'الإجابة غلط' مش كافية نعرف نصلحها. المشكلة ممكن تكون: الاسترجاع
> مجابش المعلومة الصح من الأساس، أو جابها بس اتشالت في مرحلة الفلترة، أو
> الموديل رفض يجاوب مع إن السياق كان موجود. كل سبب من دول علاجه مختلف
> تمامًا — فالتفريق ده (Fix the layer, not the symptom) بيوفر وقت تشخيص
> كبير."

---

## 🖥️ `streamlit_app.py` — الواجهة

**إيه اللي بيعمله:** واجهة chat فيها:
- Sidebar للإعدادات (اختيار المصدر، عدد الـ chunks، أسلوب الإجابة)
- فقاعات محادثة، مع Sources/Retrieved Chunks/Details تحت كل إجابة
- صفحات Knowledge Base / Analytics / Settings / About

**لو سأل "ليه استخدمتي Streamlit مش Flask/Django؟"**
> "Streamlit مصمم خصيصًا لبناء واجهات تفاعلية لمشاريع الـ Data
> Science/AI بسرعة، من غير ما تكتبي HTML/JS بايدك. بتاخد كود Python عادي
> وتحوّله لواجهة ويب تلقائيًا، وده مناسب جدًا لمشروع بالحجم والوقت ده."

---

## 🎯 أسئلة عامة متوقعة (مستوى المفهوم ككل)

**"إيه الفرق بين RAG والـ Fine-tuning؟"**
> "الـ Fine-tuning بيغيّر أوزان الموديل نفسه بتدريبه على بيانات جديدة —
> مكلف ومحتاج بيانات كتير. RAG مبيغيرش الموديل خالص، بس **بيدّيه معلومات
> إضافية وقت السؤال** عن طريق الاسترجاع. أرخص، أسرع تحديث (بس تجمعي
> بيانات جديدة وتعيدي بناء الـ vector store)، وبيدّي مصدر واضح لكل إجابة."

**"إيه أكبر تحدي واجهك في المشروع؟"**
> (جاوبي بصراحة من تجربتك، مثلاً): "مواقع كتير بتحظر الـ scraping أو
> بتعمل rate limiting (زي Hugging Face اللي رفض كل الطلبات)، فاضطريت
> أعمل نظام retry مع انتظار متزايد، وأختار مصادر بديلة لما موقع يفشل
> تمامًا."

**"لو عايزة تحسّني الدقة أكتر، هتعملي إيه؟"**
> "أزوّد حجم البيانات، أجرب موديل embeddings أكبر وأدق، أضبط الـ
> `HYBRID_ALPHA` بتجارب أكتر، أو أضيف مرحلة **re-ranking** بعد الاسترجاع
> الأولي عشان أرتب النتايج بدقة أعلى قبل ما تدخل الـ prompt."

---

## 💡 نصيحة أخيرة للمناقشة

متحاوليش تحفظي كل حاجة كلمة كلمة. افهمي **السبب** ورا كل قرار (ليه
overlap، ليه hybrid، ليه فصلنا الـ context building عن الـ retrieval)،
لأن أسئلة الدكتور غالبًا بتكون "ليه عملتي كده؟" مش "إيه اللي عملتيه؟" —
وده بالظبط اللي الدليل ده مبني عليه.
